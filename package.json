{
  "name": "lucia",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "jest",
    "build": "echo 'Build simulado'"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "jest": "^29.0.0",
    "jest-junit": "^13.0.0"
  },
  "jest": {
    "reporters": [
      "default",
      ["jest-junit", { "outputDirectory": "test-results", "outputName": "results.xml" }]
    ]
  }
}
